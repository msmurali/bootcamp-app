export interface formData {
  username: string;
  email: string;
  passwordGroup: { [key: string]: any };
  profession: string;
  course: string;
  addressGroup: { [key: string]: any };
  phoneNumber: string;
  subscribe: boolean;
}
